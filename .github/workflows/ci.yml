name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

env:
  GO_VERSION: "1.26"

jobs:
  lint:
    name: Lint & format
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-go@v7
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: true
      - name: gofmt
        run: |
          out=$(gofmt -l .)
          if [ -n "$out" ]; then
            echo "::error::gofmt found unformatted files:"; echo "$out"; exit 1
          fi
      - name: go vet
        run: go vet ./...
      - name: golangci-lint
        # v7+ is required for golangci-lint v2 configs (.golangci.yml is v2).
        uses: golangci/golangci-lint-action@v9
        with:
          # Pinned, not `latest`. golangci-lint bundles its own gofumpt, so a
          # release that bumps it changes what counts as "formatted" and turns
          # an unrelated commit red. Bump this deliberately, with the reformat
          # in the same commit.
          version: v2.12.2
          # The .golangci.yml depguard rules enforce the §4 import-direction law.

  generate:
    name: Generated code is current
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-go@v7
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: true
      - name: go generate
        run: go generate ./...
      - name: No diff (regenerate & commit if this fails)
        run: git diff --exit-code || (echo "::error::generated files are stale — run 'go generate ./...'"; exit 1)

  test:
    name: Test (race)
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-go@v7
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: true
      # The P2 memory gates skip themselves under -race and are enforced by the
      # `perf` job below instead; -race legitimately makes heap accounting
      # meaningless. Everything else in the suite runs here.
      - name: Test with the race detector
        run: go test -race -covermode=atomic -coverprofile=coverage.out ./...
      - name: Coverage summary
        if: matrix.os == 'ubuntu-latest'
        run: go tool cover -func=coverage.out | tail -1

  perf:
    name: Perf gates (P2 memory bounds)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-go@v7
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: true
      # Proves invariant P2: peak heap is a function of configuration, not
      # input size. This job is the ONLY place those assertions actually
      # execute — TestPeakMemoryConfigBound and TestHeavyPeakMemory gate on
      # `raceEnabled` and skip in the -race job above, so without this job P2
      # is unguarded in CI. Do NOT add -race here; that is what disarms them.
      #
      # Runs alone (its own job, one package) so the scans are not competing
      # with other packages' tests for CPU — memory sampling is measurably
      # noisier under that contention. -v keeps the observed peak-memory ratio
      # visible in the log even when the gate passes, so the trend is readable.
      - name: P2 memory bounds (no -race)
        run: go test -count=1 -v ./internal/perf/

  build-matrix:
    name: Cross-compile (CGO_ENABLED=0)
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        goos: [linux, darwin, windows]
        goarch: [amd64, arm64]
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-go@v7
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: true
      # Proves invariant P8: the release binary builds statically for every
      # target with no CGO. A regression that reintroduces a C dependency
      # fails here, not at release time.
      - name: Build
        env:
          CGO_ENABLED: "0"
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
        run: go build -o /dev/null ./cmd/airom

  sdk-python:
    name: Python SDK
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python: ["3.10", "3.13"]
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-go@v7
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: true
      - uses: actions/setup-python@v7
        with:
          python-version: ${{ matrix.python }}
      - name: Install the SDK
        working-directory: sdk/python
        run: pip install -e ".[dev]"
      # The suite builds the real binary and scans with it: a wrapper tested
      # only against mocks proves nothing about the contract it wraps.
      - name: Test
        working-directory: sdk/python
        run: pytest -q
      - name: Lint & type-check
        working-directory: sdk/python
        run: |
          ruff check .
          mypy
      # The binary ships as a SCRIPT (.data/scripts/), which is what puts `airom`
      # on PATH after `pip install airom` instead of leaving it reachable only
      # via `import airom`.
      - name: Build a wheel (installs the CGO-free binary as the airom command)
        working-directory: sdk/python
        run: |
          pip install build
          python -m build --wheel
          python - <<'PY'
          import glob, sys, zipfile
          whl = glob.glob("dist/*.whl")[0]
          names = zipfile.ZipFile(whl).namelist()
          scripts = [
              n for n in names
              if ".data/scripts/" in n and n.rsplit("/", 1)[-1].startswith("airom")
          ]
          if not scripts:
              sys.exit(f"{whl} installs no airom script -> `pip install` gives no command")
          if [n for n in names if "/_bin/" in n]:
              sys.exit(f"{whl} also ships the binary as package data (duplicated)")
          print(f"ok: {whl} -> {scripts}")
          PY

  fuzz:
    name: Fuzz smoke
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-go@v7
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: true
      # Short burst of every Fuzz* target (binary parsers eat untrusted
      # bytes, §13). Committed testdata/fuzz corpora are the regression seeds.
      - name: Fuzz targets (15s each)
        run: make fuzz FUZZ_TIME=15s
