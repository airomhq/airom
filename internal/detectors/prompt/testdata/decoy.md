# Project notes

This is an ordinary markdown file at the repository root. It is not under a
prompts directory and has no prompt extension, so the selector must reject it
before it ever reaches the detector.
