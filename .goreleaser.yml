# goreleaser configuration for AIROM (schema v2).
# Static, CGO-free binaries for every target (invariant P8), checksummed and
# keyless-cosign-signed.
version: 2

project_name: airom

before:
  hooks:
    - go mod download
    - go generate ./...

builds:
  - id: airom
    main: ./cmd/airom
    binary: airom
    # P8: CGO_ENABLED=0 everywhere — one static binary, no C toolchain.
    env:
      - CGO_ENABLED=0
    flags:
      - -trimpath
    ldflags:
      - -s -w
      - -X main.version={{ .Version }}
      # ShortCommit, not Commit: the Makefile stamps `git rev-parse --short` and
      # the build-info fallback abbreviates too, so a full 40-char SHA here would
      # make the release binary the odd one out — in the one string every user
      # reads, and the one embedded in every AIBOM it emits.
      - -X main.commit={{ .ShortCommit }}
      - -X main.date={{ .Date }}
    goos: [linux, darwin, windows]
    goarch: [amd64, arm64]
    mod_timestamp: "{{ .CommitTimestamp }}" # reproducible builds

archives:
  - id: airom
    name_template: >-
      {{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}
    format_overrides:
      - goos: windows
        formats: [zip]
    files:
      - LICENSE
      - NOTICE
      - README.md
      # docs/* , not docs/**/* : the ** requires at least one directory level, so
      # against a flat docs/ it matched nothing and the archives silently shipped
      # without a single doc. `goreleaser release --snapshot` logs "no files
      # matched" for this and exits 0, so it never failed anything.
      - docs/*

checksum:
  name_template: "checksums.txt"
  algorithm: sha256

# Keyless signing of the checksums file (and thereby every artifact) via
# Sigstore/cosign OIDC — the CI job has id-token: write.
signs:
  - cmd: cosign
    signature: "${artifact}.sig"
    certificate: "${artifact}.pem"
    args:
      - sign-blob
      - "--output-signature=${signature}"
      - "--output-certificate=${certificate}"
      - "${artifact}"
      - "--yes"
    artifacts: checksum
    output: true

# No per-archive SBOMs. They were six near-duplicate syft documents that buried
# the six binaries people actually came for: 19 assets, of which 6 were what you
# download. Anyone auditing a release can run syft against the archive and get
# the same document, pinned to the same checksum.

changelog:
  sort: asc
  use: github
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"
      - "^ci:"
      - "^release:"
      - "Merge pull request"
  groups:
    - title: Features
      regexp: '^.*?feat(\(.+\))??!?:.+$'
      order: 0
    - title: Bug fixes
      regexp: '^.*?fix(\(.+\))??!?:.+$'
      order: 1
    - title: Others
      order: 999

release:
  github:
    owner: airomhq
    name: airom
  draft: false
  prerelease: auto
  footer: |
    ---
    Verify the checksums signature (keyless cosign):

    ```
    cosign verify-blob \
      --certificate checksums.txt.pem \
      --signature checksums.txt.sig \
      --certificate-identity-regexp 'https://github.com/airomhq/airom' \
      --certificate-oidc-issuer https://token.actions.githubusercontent.com \
      checksums.txt
    ```
